meta {
  name: 로그아웃
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

docs {
  ```json
  {
    "message": "로그아웃되었습니다."
  }
  ```
}
